import { RnBackgroundDirective } from './rn-background.directive';

describe('RnBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new RnBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
